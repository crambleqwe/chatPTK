import React from 'react';
import {Text} from "react-native";
const HomeScreen = () => {
    return (
        // Компонент для экрана Home
        <Text>Home Screen</Text>
    );
};

export default HomeScreen